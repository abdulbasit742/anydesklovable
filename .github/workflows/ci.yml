name: RemoteDesk Dashboard CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: remotedesk-dashboard-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-build-test:
    name: Validate, lint, and build
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run local CI gates
        run: npm run ci:local
