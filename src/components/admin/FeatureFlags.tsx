export function FeatureFlags({ flags }: { flags: Array<{ id: string; name: string; enabled: boolean; description: string; rolloutPercentage: number }> }) { return <div className="bg-white rounded-lg shadow p-6"><h3 className="text-lg font-medium mb-4">Feature Flags</h3><div className="space-y-3">{flags.map(flag => <div key={flag.id} className="flex items-center justify-between p-3 border rounded-lg"><div><p className="font-medium text-sm">{flag.name}</p><p className="text-xs text-gray-500">{flag.description}</p></div><div className="flex items-center space-x-3"><span className="text-xs text-gray-500">{flag.rolloutPercentage}%</span><button className={`relative inline-flex h-6 w-11 items-center rounded-full ${flag.enabled ? "bg-blue-600" : "bg-gray-200"}`}><span className={`inline-block h-4 w-4 transform rounded-full bg-white transition ${flag.enabled ? "translate-x-6" : "translate-x-1"}`} /></button></div></div>)}</div></div>; }
