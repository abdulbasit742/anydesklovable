export function UserGrowthChart({ data }: { data: Array<{ date: string; users: number; newUsers: number }> }) { return <div className="bg-white rounded-lg shadow p-4"><h3 className="font-medium mb-4">User Growth</h3><div className="grid grid-cols-3 gap-4 mb-4"><div className="text-center"><p className="text-2xl font-bold">{data[data.length - 1]?.users.toLocaleString()}</p><p className="text-xs text-gray-500">Total Users</p></div><div className="text-center"><p className="text-2xl font-bold text-green-600">+{data.reduce((s, d) => s + d.newUsers, 0)}</p><p className="text-xs text-gray-500">New This Period</p></div><div className="text-center"><p className="text-2xl font-bold">{((data[data.length - 1]?.newUsers / data[data.length - 2]?.newUsers - 1) * 100).toFixed(1)}%</p><p className="text-xs text-gray-500">Growth Rate</p></div></div></div>; }
