export function ConnectionStats({ stats }: { stats: { totalConnections: number; avgDuration: number; peakHour: number; bandwidth: number } }) { return <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">{[{ label: "Total Connections", value: stats.totalConnections.toLocaleString() }, { label: "Avg Duration", value: `${Math.round(stats.avgDuration / 60)}min` }, { label: "Peak Hour", value: `${stats.peakHour}:00` }, { label: "Bandwidth", value: `${stats.bandwidth.toFixed(1)} GB` }].map((stat, i) => <div key={i} className="bg-white rounded-lg shadow p-4 text-center"><p className="text-2xl font-bold text-gray-900">{stat.value}</p><p className="text-sm text-gray-500">{stat.label}</p></div>)}</div>; }
