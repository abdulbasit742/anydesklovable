export function RevenueChart({ data }: { data: Array<{ month: string; mrr: number; arr: number }> }) { const maxMRR = Math.max(...data.map(d => d.mrr)); return <div className="bg-white rounded-lg shadow p-4"><h3 className="font-medium mb-4">Revenue Overview</h3><div className="flex items-end space-x-2 h-40">{data.map((point, i) => <div key={i} className="flex-1 flex flex-col items-center"><div className="w-full bg-blue-500 rounded-t opacity-80" style={{ height: `${(point.mrr / maxMRR) * 100}%` }} /><span className="text-xs text-gray-400 mt-1">{point.month}</span></div>)}</div><div className="mt-4 flex justify-between text-sm"><span className="text-gray-500">MRR: ${data[data.length - 1]?.mrr.toLocaleString()}</span><span className="text-gray-500">ARR: ${data[data.length - 1]?.arr.toLocaleString()}</span></div></div>; }
