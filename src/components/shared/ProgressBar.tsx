export function ProgressBar({ value, max = 100, color = "blue", showLabel = true }: { value: number; max?: number; color?: string; showLabel?: boolean }) { const percentage = Math.min((value / max) * 100, 100); return <div><div className="flex justify-between mb-1">{showLabel && <span className="text-sm text-gray-600">{percentage.toFixed(0)}%</span>}</div><div className="w-full bg-gray-200 rounded-full h-2"><div className={`bg-${color}-600 h-2 rounded-full transition-all`} style={{ width: `${percentage}%` }} /></div></div>; }
