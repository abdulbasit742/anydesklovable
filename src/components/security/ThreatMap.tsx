export function ThreatMap({ threats }: { threats: Array<{ id: string; type: string; level: string; source: string; timestamp: string }> }) { const levelColors: Record<string, string> = { critical: "text-red-600 bg-red-50", high: "text-orange-600 bg-orange-50", medium: "text-yellow-600 bg-yellow-50", low: "text-blue-600 bg-blue-50" }; return <div className="space-y-2">{threats.map(threat => <div key={threat.id} className={`p-3 rounded-lg ${levelColors[threat.level] || "bg-gray-50"}`}><div className="flex justify-between"><span className="font-medium text-sm">{threat.type}</span><span className="text-xs uppercase font-bold">{threat.level}</span></div><p className="text-xs mt-1">Source: {threat.source} • {threat.timestamp}</p></div>)}</div>; }
