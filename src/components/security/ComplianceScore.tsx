export function ComplianceScore({ score, standards }: { score: number; standards: Array<{ name: string; score: number; status: string }> }) { const color = score >= 90 ? "text-green-600" : score >= 70 ? "text-yellow-600" : "text-red-600"; return <div className="bg-white rounded-lg shadow p-6"><div className="text-center mb-4"><p className={`text-4xl font-bold ${color}`}>{score}%</p><p className="text-sm text-gray-500">Overall Compliance Score</p></div><div className="space-y-3">{standards.map((std, i) => <div key={i} className="flex items-center justify-between"><span className="text-sm">{std.name}</span><div className="flex items-center space-x-2"><div className="w-24 bg-gray-200 rounded-full h-2"><div className="bg-blue-600 h-2 rounded-full" style={{ width: `${std.score}%` }} /></div><span className="text-xs text-gray-500">{std.score}%</span></div></div>)}</div></div>; }
