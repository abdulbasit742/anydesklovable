export function DeviceCard({ device }: { device: { id: string; name: string; type: string; os: string; status: string; lastSeen: string; cpu?: number; ram?: number } }) { const statusColors: Record<string, string> = { online: "bg-green-500", offline: "bg-red-500", maintenance: "bg-yellow-500" }; return <div className="bg-white rounded-lg shadow p-4 hover:shadow-md transition-shadow"><div className="flex items-center justify-between"><div className="flex items-center space-x-3"><div className={`w-3 h-3 rounded-full ${statusColors[device.status] || "bg-gray-500"}`} /><div><h3 className="font-medium text-gray-900">{device.name}</h3><p className="text-sm text-gray-500">{device.os} • {device.type}</p></div></div></div>{device.cpu !== undefined && <div className="mt-3 grid grid-cols-2 gap-2"><div className="text-xs text-gray-500">CPU: {device.cpu}%</div><div className="text-xs text-gray-500">RAM: {device.ram}%</div></div>}</div>; }
