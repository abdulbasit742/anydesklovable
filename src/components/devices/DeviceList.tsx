export function DeviceList({ devices, onSelect }: { devices: any[]; onSelect: (device: any) => void }) { return <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">{devices.map(device => <div key={device.id} onClick={() => onSelect(device)} className="cursor-pointer"><DeviceCard device={device} /></div>)}</div>; }
function DeviceCard({ device }: { device: any }) { return <div className="bg-white rounded-lg shadow p-4 hover:shadow-md"><div className="flex items-center space-x-3"><div className={`w-3 h-3 rounded-full ${device.status === "online" ? "bg-green-500" : "bg-red-500"}`} /><div><h3 className="font-medium">{device.name}</h3><p className="text-sm text-gray-500">{device.os}</p></div></div></div>; }
