export function MetricsChart({ title, data, type = "line" }: { title: string; data: Array<{ label: string; value: number }>; type?: "line" | "bar" }) { const max = Math.max(...data.map(d => d.value)); return <div className="bg-white rounded-lg shadow p-4"><h3 className="font-medium text-gray-900 mb-4">{title}</h3><div className="flex items-end space-x-1 h-32">{data.map((point, i) => <div key={i} className="flex-1 flex flex-col items-center"><div className="w-full bg-blue-500 rounded-t" style={{ height: `${(point.value / max) * 100}%` }} /><span className="text-xs text-gray-400 mt-1">{point.label}</span></div>)}</div></div>; }
