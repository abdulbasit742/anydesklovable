export function AlertsList({ alerts }: { alerts: Array<{ id: string; severity: string; message: string; deviceName: string; triggeredAt: string }> }) { const severityColors: Record<string, string> = { critical: "border-red-500 bg-red-50", warning: "border-yellow-500 bg-yellow-50", info: "border-blue-500 bg-blue-50" }; return <div className="space-y-3">{alerts.map(alert => <div key={alert.id} className={`border-l-4 p-4 rounded-r-lg ${severityColors[alert.severity] || "border-gray-500 bg-gray-50"}`}><div className="flex justify-between"><div><p className="font-medium text-gray-900">{alert.message}</p><p className="text-sm text-gray-500">{alert.deviceName} • {alert.triggeredAt}</p></div><span className="text-xs font-medium uppercase">{alert.severity}</span></div></div>)}</div>; }
