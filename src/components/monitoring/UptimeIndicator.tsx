export function UptimeIndicator({ percentage, days }: { percentage: number; days: Array<{ date: string; status: "up" | "down" | "partial" }> }) { const statusColors = { up: "bg-green-500", down: "bg-red-500", partial: "bg-yellow-500" }; return <div className="bg-white rounded-lg shadow p-4"><div className="flex justify-between items-center mb-3"><h3 className="font-medium">Uptime</h3><span className="text-2xl font-bold text-green-600">{percentage.toFixed(2)}%</span></div><div className="flex space-x-0.5">{days.map((day, i) => <div key={i} className={`flex-1 h-8 rounded-sm ${statusColors[day.status]}`} title={`${day.date}: ${day.status}`} />)}</div></div>; }
