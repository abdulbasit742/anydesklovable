export function DiagnosticResult({ result }: { result: { diagnosis: string; confidence: number; fixes: string[]; autoFixable: boolean } }) { return <div className="bg-white rounded-lg shadow p-4"><div className="flex items-center justify-between mb-3"><h4 className="font-medium">Diagnosis</h4><span className={`text-xs px-2 py-1 rounded-full ${result.confidence > 0.8 ? "bg-green-100 text-green-800" : "bg-yellow-100 text-yellow-800"}`}>{(result.confidence * 100).toFixed(0)}% confidence</span></div><p className="text-gray-700 mb-3">{result.diagnosis}</p><h5 className="text-sm font-medium mb-2">Suggested Fixes:</h5><ul className="space-y-1">{result.fixes.map((fix, i) => <li key={i} className="flex items-center text-sm"><span className="text-blue-500 mr-2">→</span>{fix}</li>)}</ul>{result.autoFixable && <button className="mt-3 w-full py-2 bg-green-600 text-white rounded-lg hover:bg-green-700">Auto-Fix Now</button>}</div>; }
