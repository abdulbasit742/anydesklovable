export function InvoiceTable({ invoices }: { invoices: Array<{ id: string; number: string; date: string; amount: number; status: string }> }) { return <table className="min-w-full divide-y divide-gray-200"><thead><tr><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Invoice</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Date</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Amount</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th></tr></thead><tbody className="divide-y divide-gray-200">{invoices.map(inv => <tr key={inv.id}><td className="px-4 py-3 text-sm">{inv.number}</td><td className="px-4 py-3 text-sm">{inv.date}</td><td className="px-4 py-3 text-sm font-medium">${inv.amount.toFixed(2)}</td><td className="px-4 py-3"><span className={`px-2 py-1 rounded-full text-xs ${inv.status === "paid" ? "bg-green-100 text-green-800" : "bg-yellow-100 text-yellow-800"}`}>{inv.status}</span></td></tr>)}</tbody></table>; }
