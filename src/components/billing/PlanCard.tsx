export function PlanCard({ plan, current, onSelect }: { plan: { name: string; price: number; features: string[]; popular?: boolean }; current?: boolean; onSelect: () => void }) { return <div className={`rounded-lg border-2 p-6 ${current ? "border-blue-500 bg-blue-50" : plan.popular ? "border-purple-500" : "border-gray-200"}`}>{plan.popular && <span className="bg-purple-500 text-white text-xs px-2 py-1 rounded-full">Most Popular</span>}<h3 className="text-xl font-bold mt-2">{plan.name}</h3><p className="text-3xl font-bold mt-2">${plan.price}<span className="text-sm text-gray-500">/mo</span></p><ul className="mt-4 space-y-2">{plan.features.map((f, i) => <li key={i} className="flex items-center text-sm"><span className="text-green-500 mr-2">✓</span>{f}</li>)}</ul><button onClick={onSelect} className={`w-full mt-6 py-2 rounded-lg ${current ? "bg-gray-300 cursor-not-allowed" : "bg-blue-600 text-white hover:bg-blue-700"}`}>{current ? "Current Plan" : "Select Plan"}</button></div>; }
