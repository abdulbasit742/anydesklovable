export function DeploymentProgress({ deployment }: { deployment: { name: string; status: string; total: number; completed: number; failed: number } }) { const progress = deployment.total > 0 ? ((deployment.completed + deployment.failed) / deployment.total) * 100 : 0; return <div className="bg-white rounded-lg shadow p-4"><div className="flex justify-between items-center mb-2"><h4 className="font-medium">{deployment.name}</h4><span className={`text-xs px-2 py-1 rounded-full ${deployment.status === "completed" ? "bg-green-100 text-green-800" : "bg-blue-100 text-blue-800"}`}>{deployment.status}</span></div><div className="w-full bg-gray-200 rounded-full h-2 mb-2"><div className="bg-blue-600 h-2 rounded-full" style={{ width: `${progress}%` }} /></div><div className="flex justify-between text-xs text-gray-500"><span>{deployment.completed} completed</span><span>{deployment.failed} failed</span><span>{deployment.total - deployment.completed - deployment.failed} pending</span></div></div>; }
