export function GroupTree({ groups, onSelect }: { groups: Array<{ id: string; name: string; deviceCount: number; children?: any[] }>; onSelect: (group: any) => void }) { return <div className="space-y-1">{groups.map(group => <div key={group.id}><button onClick={() => onSelect(group)} className="flex items-center justify-between w-full px-3 py-2 text-sm rounded hover:bg-gray-100"><span className="flex items-center"><span className="mr-2">📁</span>{group.name}</span><span className="text-gray-400 text-xs">{group.deviceCount}</span></button>{group.children && <div className="ml-4"><GroupTree groups={group.children} onSelect={onSelect} /></div>}</div>)}</div>; }
